const Header = () => {
  return (
    <header
      style={{ backgroundColor: "green", fontWeight: "bold", color: "red" }}
    >
      Header du main et de Tom
    </header>
  );
};

export default Header;
